Strings in C++ are differentiated from strings in c as follows:
In C, the strings are character arrays and must end with \0 otherwise they are not recognised as strings
In C++, strings are classes and must not end with \0 and have many member functions.
